{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: 'Date Range'|t('smartlink-manager'),
    id: 'dateRange',
    name: 'dateRange',
    value: widget.dateRange,
    options: lrDateRangeOptions('assoc')
}) }}

{{ forms.selectField({
    label: 'Site'|t('smartlink-manager'),
    id: 'siteId',
    name: 'siteId',
    value: widget.siteId,
    options: siteOptions,
}) }}
